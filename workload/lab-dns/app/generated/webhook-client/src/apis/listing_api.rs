/*
 * External DNS Webhook Server
 *
 * Implements the external DNS webhook endpoints.
 *
 * The version of the OpenAPI document: v0.15.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use std::sync::Arc;
use std::borrow::Borrow;
use std::pin::Pin;
#[allow(unused_imports)]
use std::option::Option;

use hyper;
use hyper_util::client::legacy::connect::Connect;
use futures::Future;

use crate::models;
use super::{Error, configuration};
use super::request as __internal_request;

pub struct ListingApiClient<C: Connect>
    where C: Clone + std::marker::Send + Sync + 'static {
    configuration: Arc<configuration::Configuration<C>>,
}

impl<C: Connect> ListingApiClient<C>
    where C: Clone + std::marker::Send + Sync {
    pub fn new(configuration: Arc<configuration::Configuration<C>>) -> ListingApiClient<C> {
        ListingApiClient {
            configuration,
        }
    }
}

pub trait ListingApi: Send + Sync {
    fn get_records(&self, ) -> Pin<Box<dyn Future<Output = Result<Vec<models::Endpoint>, Error>> + Send>>;
}

impl<C: Connect>ListingApi for ListingApiClient<C>
    where C: Clone + std::marker::Send + Sync {
    #[allow(unused_mut)]
    fn get_records(&self, ) -> Pin<Box<dyn Future<Output = Result<Vec<models::Endpoint>, Error>> + Send>> {
        let mut req = __internal_request::Request::new(hyper::Method::GET, "/records".to_string())
        ;

        req.execute(self.configuration.borrow())
    }

}
